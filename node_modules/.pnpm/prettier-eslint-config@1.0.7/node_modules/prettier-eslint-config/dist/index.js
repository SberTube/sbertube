#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
// @ts-ignore
const package_json_1 = tslib_1.__importDefault(require("../package.json"));
const sade_1 = tslib_1.__importDefault(require("sade"));
const init_1 = tslib_1.__importDefault(require("./tasks/init"));
const version = package_json_1.default.version; //版本号
const cliName = Object.keys(package_json_1.default.bin)[0]; //包名称
const prog = (0, sade_1.default)(cliName);
prog.version(version).option('-c, --config', '就是一些我没有想好的动态配置');
prog
    .command('init')
    .describe('初始化项目中的eslint配置和prettier格式化内容')
    .example('lint init')
    .action(async () => {
    // 初始化
    await (0, init_1.default)();
});
prog.parse(process.argv);
