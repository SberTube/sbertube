"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPrompt = void 0;
const tslib_1 = require("tslib");
const inquirer_1 = tslib_1.__importDefault(require("inquirer"));
const index_1 = require("./index");
const prompt = async (describe, choices, type) => {
    return inquirer_1.default
        .prompt([{ type: type || 'list', name: describe, choices: choices }])
        .catch(error => {
        if (error.isTtyError) {
            index_1.logger.error(error);
        }
        else {
            index_1.logger.error(error);
        }
        return error;
    });
};
/**
 * 创建prompt
 * @param describe 描述
 * @param selectedList 可选择的列表
 * @returns 选择的值
 */
const createPrompt = async (describe, selectedList) => {
    const answer = await prompt(describe, selectedList);
    return answer[describe];
};
exports.createPrompt = createPrompt;
exports.default = exports.createPrompt;
